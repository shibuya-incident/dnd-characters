services:
  db:
    image: postgres:18
    container_name: dnd-characters-dev
    environment:
      POSRTGRES_DB: dnd_characters
      POSTGRE_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: